export interface Ilanguage {
    code: string;
    lang: string;
}

export interface IShakeyProps {
    data: (string | number)[][],
    options: any
}

export interface IFormData {
    from: string;
    to: string;
    value: string;
}